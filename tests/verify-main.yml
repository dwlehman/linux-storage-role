---
- name: Verify the mountpoint of present device
  include_tasks: "verify-mount.yml"

- name: Verify the size of present lvm device
  include_tasks: "verify-lvm-size.yml"
  when: state == "present" and device_type == "lvm"
